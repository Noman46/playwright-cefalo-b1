name: Print My Name

on:
  workflow_dispatch: # Allows manual triggering of the workflow
  # Uncomment the lines below to schedule the workflow to run every 2 minutes
  # schedule:
  #   - cron: "*/2 * * * *"

jobs:
  print_name:
    runs-on: ubuntu-latest
    steps:
      - name: Print Your Name
        run: echo "My name is Noman"
