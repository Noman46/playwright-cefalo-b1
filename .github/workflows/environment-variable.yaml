name: Environemnt Variables
on:
  workflow_dispatch:

env:
    BASE_l_ENV_URL: "My Name is noman"

jobs:
  echo_name:
    runs-on: ubuntu-latest
    environment: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Echo Variables Name
        run: echo $BASE_l_ENV_URL
      - name: Echo env
        run: echo ${{ vars.BASE_ENV_URL }}